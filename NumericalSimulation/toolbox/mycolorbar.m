function c = mycolorbar()

c = colorbar('FontSize',20,'linewidth',1.5,'FontName','Arial','fontweight','b');
% ax = gca;
% axpos = ax.Position;
% cpos = c.Position;  % [x y width heigth]
% cpos(1) = 1.1*cpos(1); 
% cpos(2) = 1.1*cpos(2); 
% cpos(3) = 0.8*cpos(3);   % width 
% cpos(4) = 0.9*cpos(4);   % height
% c.Position = cpos;
% ax.Position = axpos;

end